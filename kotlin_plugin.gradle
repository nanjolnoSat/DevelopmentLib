apply from: "$rootDir/dependencies_list.gradle"

if (useKotlin) {
    apply plugin: "kotlin-android"
    apply plugin: "kotlin-android-extensions"
    apply plugin: "kotlin-kapt"

    dependencies {
        implementation deps.kotlinCore
        implementation deps.androidCoreKtx
    }
}